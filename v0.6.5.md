# v0.6.5 — Task Capsule & Counterfactual Skill Evaluation

## 目标

v0.6.5 为 Skill Evolution 增加可靠实验层：从同一不可变初始状态分叉 Baseline 与 Candidate，只改变显式声明的 Skill 变量，重新执行完整 Agent 轨迹并形成可比较证据。

```text
Task Capsule
    ↓
Fork identical worlds
    ├─ Baseline × N
    └─ Candidate Skill × N
    ↓
Measured evidence + optional blind semantic evidence
    ↓
REJECTED / INSUFFICIENT_EVIDENCE / NEEDS_REVIEW / PROMOTABLE
    ↓
Manual promotion
```

## 已实现边界

- `ExecutionCapsule(kind=task)`；
- Workspace 与 Active Skill 的 SHA256 内容寻址快照；
- Git commit、dirty diff hash 和 untracked 文件元数据（存在 Git 时）；
- Capsule 完整性、Fidelity 和生命周期；
- `mutation_type=skill` 的显式 Experiment Variant；
- 恢复并重新执行，而非重放旧 Tool Calls；
- 默认 Baseline/Candidate 各 3 次；
- 完整 Run Evidence 和多维 Counterfactual Report；
- 可拒判、可检测位置偏差的语义 Judge 接口；
- 人工晋升门和强于 Historical Replay 的 Counterfactual Evidence Gate。

## 尚未实现

- Recorded Model/Tool Replay；
- 网络录制、外部世界冻结与 Mock Replay；
- 自动选择所有适合作为基线的任务；
- Shapley 多变量归因；
- Workflow、Plugin、Planner、Memory Policy 等非 Skill Mutation。

普通任务不会自动永久保存 Capsule。要获得 `FULL` 的任务前状态，应在任务仍为首次排队状态时执行 `capsule capture`；任务完成后补抓会标为 `PARTIAL`。无法解析精确 Docker Image Digest 时同样降为 `PARTIAL`。
