# AIOS 当前工作总览

> 更新时间：2026-09-05  
> 仓库：`C:\Users\15959\Desktop\AIOS\aios`  
> 冻结前基线：`2de6be7 feat: add component lineage evaluation`  
> 最终状态：以 Git 标签 `v0.9.0-alpha.6-research-freeze` 封存  
> Python 包声明版本：`0.9.0a6`（`pyproject.toml`）

## 1. 项目现在是什么

AIOS 已经从最初的“LLM 读取任务、生成 JSON 计划、调用少量文件工具”的原型，演进为一个包含以下能力的本地 Agent Runtime 与自演化实验平台：

```text
User / Web UI / CLI
        ↓
Durable Task + Event Queue
        ↓
Agent Runtime（Tool Calling、工作状态、上下文、记忆）
        ↓
Primitive / Plugin / Resource Adapter / Environment Provider / Skill
        ↓
Docker Sandbox + Authority + Versioned Storage + Trace

正常执行产生 Experience / Capsule
        ↓
Candidate / Component / Harness Mutation
        ↓
Experimental Lineage
        ↓
Counterfactual Replay（父版本 vs 子版本）
```

当前项目的核心研究目标已经不只是“让 Agent 完成任务”，而是：

> 让 Agent 能观察自己的真实运行经历，提出可继承的变化，在隔离环境中产生后代，并通过现实后果继续选择、返回或再变异。

Host 负责事实可靠性、边界、历史和可逆性；Agent 负责解释、决策和候选创作。当前 Free Runtime 实验进一步把在线 Verifier 从闭环中移出，只记录 `agent_declared_stop`，不把它伪装成客观完成。

## 2. 已完成的主要演进阶段

### v0.5：可运行基线与架构边界

- 建立 Python 包、CLI、SQLite 状态库、任务队列、结果箱、Trace、记忆和死信。
- 将模型输出从脆弱的纯 JSON 计划逐步迁移到原生 Tool Calling。
- 明确 Agent、Harness、Host、Sandbox 和权限边界。
- 支持 DeepSeek 的 OpenAI 兼容接口，并处理空输出、协议错误、序列化 Tool Call 标记等问题。

### v0.6：Skill、资源与 Runtime Hardening

- 引入版本化 Skill：候选、验证、Benchmark、晋升、回滚、弃用、Telemetry、Replay 和 Utility。
- 增加 `read/write/edit/bash` 最小原语，并允许受控 Plugin 扩展。
- 引入执行前 Capability Preflight，区分 missing、forbidden 和 needs authority。
- 统一工作区路径语义，修复 URL 被误判为宿主路径等问题。
- 增加 PDF、XLSX、CSV、HTTP Resource Adapter，以及资源范围、Observation、Coverage 与缓存机制。
- Runtime Hardening：`pipefail`、默认/最大 timeout、Cycle/Task Budget、Checkpoint continuation、依赖缓存、Context Compaction、metadata-first 资源观察。
- 修复 Windows 只读 `.git` 对象导致 Sandbox 无法清理的问题。
- 引入 Task Capsule 和父/子 Counterfactual Evaluation。

### v0.7：Component + Capability 统一抽象

- 建立 `ComponentManifest`、`ComponentRegistry`、`ComponentLineage`、`ComponentEvaluation`。
- 当前 Component 类型：
  - `primitive`
  - `skill`
  - `workflow`
  - `resource_adapter`
  - `environment_provider`
  - `plugin`
  - `kernel_component`
- 统一 `requires / provides Capability`、版本、来源、信任策略和生命周期表达。
- SkillRegistry 作为 ComponentRegistry 的兼容来源，而不是立即重写全部 Skill 系统。
- 当前只有 Skill 被声明为 Agent 可自动创作的 Component；其他类型大多仍是 Host 管理或只读登记。

### v0.8：自主 Runtime Repair 实验 Harness

- 建立 Runtime Provenance、failure-time 源码快照、源码符号索引和脱敏 Evidence Capsule。
- 将实验链拆成：

```text
Observe → Attribute → Propose → Evaluate
```

- 增加 temporal attribution consistency、failure path、patch reachability、Mutation Boundary、Evidence Catalog。
- Host 只做合同、事实一致性、边界与外部门禁，不替模型完成因果诊断。
- Task 77/79/80/83/84 等形成冻结回归案例。
- alpha.1–alpha.6 的结论：
  - Self-observation、Provenance、Source delivery、Host safety 已较稳定；
  - 因果诊断、克制性和 Patch semantic precision 仍不可靠；
  - 自主 Runtime Repair 尚未被证明。
- 后续停止围绕已知案例继续堆提示规则，等待新的 provenance-complete Future Holdout。

### v0.9：Self-Optimization、Free Runtime 与 Autonomous Lineage

- 增加 Soft Friction Experience，观察“成功但昂贵”及“无确认 Runtime 缺陷的策略失败”。
- 增加 Open Evolution Agent，使模型在隔离源码世界里自由 inspect/edit/test，而非只填结构化推理表。
- 修复 Observation reload identity：读取已有 Observation 不再制造新的 Observation。
- 增加 Harness Sensitivity：H0 structured、H1 reduced、H2 minimal-open。
- 增加 Free Runtime：在线 Verifier 退出运行闭环；状态改为 `stopped/yielded/abandoned` 等事实表达。
- 增加本地 Agent Workbench UI，区分 Verified/Free 模式，展示任务、Chat、Activity、预算和 Trace。
- 增加 Autonomous Lineage：Agent 可 `CONTINUE`、采用候选、创作 Skill Component 候选、分叉 Harness 变异、请求反事实测量或返回祖先。
- 增加 Component Set 的谱系继承快照。
- 增加谱系行为摘要、任务状态释义、当前实际配置事实，停止把祖先的自然语言 `decision.reason` 当作当前事实回灌。
- 增加谱系原生 `REQUEST_COUNTERFACTUAL_EVALUATION`。

## 3. 当前实际运行配置

以下是当前 `config.json` 的生效事实，不包含密钥和本地绝对路径：

| 项目 | 当前值 | 含义 |
|---|---|---|
| Runtime completion | `free` | Agent 声明停止即记录 `stopped`，不做在线正确性裁决 |
| Budget | `enabled=false` | 普通任务的 Cycle/Task Model、Tool、Token 配额不生效，但仍计量 |
| Model | `deepseek-v4-flash` | DeepSeek provider |
| Thinking | `enabled` | 开启推理输出模式 |
| Protocol | `tool_calling` | 使用原生 Tool Calling |
| Model timeout | `1200s` | 单次模型请求最长 20 分钟 |
| Model max output | `31296` tokens | 单次输出上限仍保留 |
| Sandbox | Docker / `python:3.12-slim` | 隔离执行后端 |
| Command timeout | 默认 `60s`，最大 `300s` | 与模型 timeout 分离 |
| Sandbox resource | 512 MB、1 CPU、128 PIDs | 容器资源限制 |
| Network | enabled，allowed domains 为空 | 允许完全出网 |
| Primitive tools | `read/write/edit/bash` | 最小工具面 |
| Skill | enabled，默认不引导内置 Skill | `bootstrap_builtins=false` |
| Skill promotion | 需要人工 | `require_human_promotion=true` |
| Legacy evolution | disabled | 旧自动触发器关闭，不等于 Autonomous Lineage 关闭 |

重要提醒：`budget.enabled=false` 只取消普通任务配额，不取消模型输出、命令 timeout、Docker 资源和权限边界。它也意味着错误循环可能持续计费。

## 4. 当前模块结构

| 层 | 主要模块 | 职责 |
|---|---|---|
| CLI/UI | `cli.py`、`webui.py`、`web/` | 任务提交、Chat、Activity、状态与演化操作 |
| Runtime | `runtime.py`、`controller.py`、`protocol.py` | Agent loop、Tool Calling、协议恢复、任务终止 |
| State | `storage.py`、`types.py` | SQLite、任务、事件、Trace、Memory、Capsule、Experiment、Lineage |
| Security | `security.py`、`sandbox.py` | Authority、Docker、路径边界、资源限制、清理 |
| Tools | `tools.py`、`plugins.py` | Primitive 与受控 Plugin |
| Resource | `resources.py`、`situation.py` | 资源发现、Adapter、Observation、Coverage、复用 |
| Capability | `capabilities.py`、`components.py` | Capability 图、Component 注册和版本投影 |
| Skill | `skills.py`、`utility.py` | Skill 生命周期、评估与效用 |
| Evolution | `evolution.py`、`self_evolution.py` | 旧 Harness/Skill 演化与 Soft Friction |
| Runtime Evolution | `runtime_evolution.py`、`runtime_provenance.py` | Runtime defect 候选、盲测与外部门禁 |
| Open Evolution | `open_evolution.py` | 开放式源码 inspect/edit/test Candidate world |
| Lineage | `lineage.py`、`lineage_behavior.py` | 自主谱系决策、行为事实、Component Set 继承 |
| Experiments | `experiments/` | Capsule、隔离重放、父子对照、语义测量 |
| Completion | `evaluation.py`、`answers.py` | Verified 模式判断与结果投影；Free 模式下作为离线仪器 |

## 5. 当前数据状态

### 任务与经验

数据库中共有 121 个普通任务：

| 状态 | 数量 |
|---|---:|
| completed | 49 |
| stopped | 27 |
| dead_letter | 25 |
| degraded | 8 |
| blocked_capability | 4 |
| yielded | 4 |
| needs_authority | 3 |
| needs_review | 1 |

`completed` 主要来自早期 Verified Runtime；`stopped` 是 Free Runtime 下的 Agent 自行停止，不能直接解释为客观完成。

### Component

当前注册并 active 的 Component 共 11 个：

| Kind | 数量 | 当前成员 |
|---|---:|---|
| primitive | 4 | read、write、edit、bash |
| resource_adapter | 4 | PDF、XLSX、CSV、HTTP reader |
| environment_provider | 2 | Docker network bridge、scientific Python 3.12 |
| plugin | 1 | search_files |
| skill | 0 | 当前没有 active Skill |
| workflow | 0 | 只有协议类型，没有运行实例 |

因此“Component 已接入谱系”不等于“所有 Component 都能被 Agent 创作”。目前真正开放的自主 Component Authoring 仍只有 `kind=skill`，且尚未成功留下候选。

### Skill 与旧 Plugin 候选

- `python -m aios ... skill list`：空。
- `python -m aios ... skill candidates`：空。
- 历史上曾自动生成并晋升 `search_files/query_tasks/query_traces/query_dead_letters` 等受限状态查询 Plugin 候选；当前 Component Set 只登记了 `search_files` Plugin。
- 多次 `AUTHOR_COMPONENT_CANDIDATE(kind=skill)` 已发生，但曾因 schema validation、Benchmark timeout 等失败，没有形成可采用 Skill。

### Capsule 与实验

- 可重放 Capsule：5 个。
- Full pre-task Capsule：3 个（Task 88、89、90）。
- Partial Capsule：2 个（Task 86、87），不能用于高保真父子对照。
- Experiment：6 completed、2 failed、2 历史 running/stale。
- Evolution Run：包含 observed、measured、candidate proposed、promoted、rejected、rollback、protocol failed、lineage evaluated 等状态。

## 6. 当前谱系

当前实验 Head：`lin_bf4054b853224d09`。

| Generation | Lineage | Parent | 变化来源 |
|---:|---|---|---|
| 0 | `lin_root` | — | Root snapshot |
| 1 | `lin_b434ddb5b1464956` | root | Candidate 8：`max_actions_per_cycle=20`，Agent fork |
| 1 | `lin_df2c74eefbeb4a31` | root | Candidate 8：另一条 adopt 分支 |
| 2 | `lin_30779d600a014ba9` | b434 | Candidate 9：`memory_context_characters=20000`，Agent fork |
| 2 | `lin_f74fcc359cfa43d2` | b434 | Candidate 9：另一条 adopt 分支 |
| 2 | `lin_45a1d32f240046fe` | b434 | Candidate 9：另一条 adopt 分支 |
| 3 | `lin_bf4054b853224d09` | 3077 | Candidate 10：read-ledger `prompt_append` |

当前 Head 继承的设置：

```text
max_actions_per_cycle = 20
memory_context_characters = 20000
prompt_append = per-task read ledger
```

由于全局 `budget.enabled=false`，`max_actions_per_cycle=20` 当前是继承事实，但不会作为运行时配额生效。

## 7. 最新父子反事实实验（Run 93）

Run 93 已成功执行 `REQUEST_COUNTERFACTUAL_EVALUATION`，比较：

```text
Parent    lin_30779d600a014ba9
Candidate lin_bf4054b853224d09（增加 read-ledger prompt）
```

### Capsule Task 90：资源复现

| 指标 | Parent | Candidate | 变化 |
|---|---:|---:|---:|
| 重复资源动作 | 3 | 0 | -3 |
| Model Calls | 22 | 23.5 | +1.5 |
| Tool Calls | 30.5 | 29 | -1.5 |
| Tokens | 285,963 | 310,992 | +25,029 |

结论：局部减少重复读取，但 Token 和模型调用略增；双方均为 `stopped`，正确性未被在线确认。

### Capsule Task 89：数据集分析

| 指标 | Parent | Candidate | 变化 |
|---|---:|---:|---:|
| 重复资源动作 | 4 | 5 | +1 |
| Model Calls | 29.5 | 107.5 | +78 |
| Tool Calls | 42.5 | 128 | +85.5 |
| Tokens | 424,961 | 1,775,305.5 | +1,350,344.5 |
| 失败工具调用 | 3 | 11.5 | +8.5 |

结论：发生明显负迁移。read-ledger 没有减少重复读取，反而显著增加推理、工具调用、失败和耗时。

### Capsule Task 88：PDF + XLSX 分析

父子各两次运行全部 `abandoned`，记录为 0 Model Calls、0 Tool Calls、无 Trace、无最终输出，但具有显著墙钟等待时间。当前 RunEvidence 没有保留 task error，因此无法从已保存报告判断放弃原因。

这四条样本不应被解释为有效的“零成本实验”，也不应进入候选效用结论。

### 综合判断

- `selection=none_measurement_only`
- `production_activated=false`
- `host_fitness_judgment=false`
- 当前谱系没有自动回退或晋升。
- read-ledger 候选没有证明跨任务有效；现有证据更支持返回 Parent，至少不支持继续推广。
- 本轮报告包含约 559 万运行 Tokens；断点恢复成功避免重复执行第一组约 119 万 Tokens。

## 8. 冻结版本状态

### 冻结前基线

GitHub `main` 当前为：

```text
2de6be7 feat: add component lineage evaluation
```

它包括 Component Lineage、Free/Unlimited 配置事实、谱系行为可观察性、Candidate Authoring、反事实评估入口及相关报告。

### 冻结版新增收尾

最终冻结提交在上述基线上纳入以下收尾内容：

- `README.md`
- `src/aios/experiments/orchestrator.py`
- `src/aios/storage.py`
- `tests/test_v065.py`

实验恢复改动涉及上述四个文件，并实现：

- 失败实验按 Capsule、父子变体和重复次数精确匹配；
- 复用已持久化的 `(variant, replicate)`；
- 复用已有 semantic judgement；
- 只执行缺失 replicate；
- Counterfactual report 使用幂等 upsert；
- 报告显式记录恢复来源。

Run 93 已在真实数据库中证明该机制能够恢复 Run 92 的 4 条运行证据。

冻结提交同时加入本总览、README 最终研究报告，统一 Python 包版本为 `0.9.0a6`，并更新项目描述。最终提交与标签均推送至 GitHub `main`。

## 9. 当前已确认的能力与未证明能力

### 已确认

- Durable Task/Event/Result/Trace/Memory/Dead Letter 工作。
- DeepSeek 原生 Tool Calling 与多类协议恢复工作。
- Docker Sandbox、工作区隔离、路径权限和受控网络工作。
- PDF/XLSX/CSV/HTTP Resource Adapter 和 Observation 体系可用。
- Capability Preflight 能阻断缺失能力与越界请求。
- Task Capsule、相同初始状态 Fork、Experiment persistence 工作。
- Component Registry 与 Capability provides/requires 表达工作。
- Free Runtime 能把 Agent stop 与 Host completion 分开。
- Experimental Lineage 能分叉、继承、绑定任务、产生候选和请求测量。
- Counterfactual Evaluation 能真实运行父子版本，且不会自动晋升生产。
- 断点恢复能避免完整重跑已经落库的 replicate。
- 当前完整测试套件：306 tests，全部通过。

### 尚未证明

- AIOS 能稳定自主修复新的 Runtime 缺陷。
- AIOS 能稳定生成语义正确、安全可达的 Runtime patch。
- AIOS 能从 Experience 稳定创作并保留有用 Skill。
- AIOS 能自主选择出跨任务更优的 Harness 后代。
- 当前 read-ledger 后代优于 Parent。
- Free Runtime 中 `stopped` 输出的真实正确性。
- Workflow、Plugin、Resource Adapter、Environment Provider 的自主创作与演化闭环。

## 10. 目前最重要的已知问题

1. **实验错误证据丢失**  
   `RuntimeVariantRunner` 对 `abandoned` 任务没有把 `finished.error` 和终止原因写入 RunEvidence；隔离 world 删除后无法追因。

2. **无效运行仍被聚合**  
   0 Calls、无 Trace、无输出的 abandoned 运行仍进入 median/delta，可能制造“零成本”假象。

3. **当前 Head 出现负迁移**  
   read-ledger 在 Task 89 上使 Token 和调用数大幅上升。

4. **Free 模式缺少在线质量结论**  
   `stopped` 只能说明 Agent 停止，不能说明目标完成；需要离线 shadow measurement 或后续世界反馈。

5. **实验成本缺少独立硬边界**  
   全局 Budget 关闭后，单次候选重放可使用数百万 Tokens。实验应有独立于普通任务的最大费用/调用/墙钟保护。

6. **Skill Authoring 尚未闭环**  
   动作已经存在，但当前 Skill 和 Skill Candidate 都为空；生成、验证和 Benchmark 仍不够稳定。

7. **Component Evolution 只开放了一小部分**  
   Component 协议支持多种 kind，但 Agent 目前只可创作 Skill；Harness 仍通过专用 setting mutation，而不是完全统一的 Component mutation。

8. **历史 stale Experiment**  
   数据库仍有 2 个 `running` 且 0 runs 的旧实验记录，需要显式 stale/recovery 状态，而不是永久显示运行中。

9. **旧 Evolution 与新 Lineage 容易混淆**  
    当前 `evolution.enabled=false` 关闭的是旧自动触发机制，不代表 `evolution lineage-run` 不可用。

## 11. 与 Hermes 的关系

Hermes 和 AIOS 都试图把运行经验转化为持久能力，但重点不同：

- Hermes 的主循环偏向 Memory/Skill 的轻量在线沉淀，实用性强、变化频繁。
- AIOS 重点是 Candidate、不可变 Capsule、父子谱系和反事实测量，研究可证伪的版本演化。
- AIOS 当前的优势是谱系和实验结构；弱点是候选产生较重、评估昂贵、有效性尚未证明。

适合吸收的思路是：

```text
高频轻量内环：任务经验 → Memory / Skill
低频重型外环：跨任务证据 → Component/Harness 后代 → 反事实实验
```

如果谱系机制不能证明比直接 Skill 学习带来额外收益，AIOS 就可能退化成“更复杂、更昂贵的 Hermes”。

## 12. 建议的下一步顺序

### P0：先修测量正确性

1. 在 RunEvidence 中保存 task error、terminal reason、Trace 缺失原因和实际 actor。
2. 将无 Trace、无调用、无输出的 abandoned run 标记为 `invalid_run`。
3. Aggregate 排除 invalid run，并在样本不足时输出 `insufficient_valid_replicates`。
4. 清理或正式标记历史 stale experiments。

### P0：处理当前负迁移谱系

让 Agent读取 Run 93 后自行决策。基于当前人工分析，合理动作是 `RETURN lin_30779d600a014ba9`，而不是继续扩大 read-ledger。

### P1：给实验增加独立成本边界

即使普通任务 `budget.enabled=false`，Counterfactual Experiment 仍应有独立的：

- 最大 Tokens；
- 最大模型调用；
- 最大墙钟时间；
- 单 replicate 停止条件；
- 跨 replicate 总成本上限。

### P1：继续正常收集新 Experience

- 不再围绕 Task 77/79/80/83/84 定制提示。
- 新任务执行前优先 Capture Full Capsule。
- 收集 5–10 个真实 Soft Friction / Strategy Adaptation Experience。
- 只在跨任务重复模式出现后创作长期 Skill 或 Strategy Candidate。

### P2：完成轻量 Skill 内环

- 允许任务内发现可复用流程后生成隔离 Skill Candidate。
- 第一阶段仅做小型确定性测试，不直接采用。
- 有跨任务证据后再进入 Capsule counterfactual。
- 保留人工晋升开关。

### P3：再统一更广的 Component Evolution

只有在 Skill 闭环稳定后，再逐步开放 workflow；Plugin/Adapter/Provider/Kernel 仍应根据 Trust Policy 保持更严格边界。

## 13. 常用操作命令

```powershell
# 启动 Runtime
python -m aios --config config.json run

# 启动本地 UI
python -m aios --config config.json ui

# 提交普通任务
python -m aios --config config.json task submit "任务内容"

# 提交并绑定实验谱系
python -m aios --config config.json task submit "任务内容" --lineage lin_bf4054b853224d09

# 查看任务和结果
python -m aios --config config.json task list
python -m aios --config config.json result show <task_id>
python -m aios --config config.json result answer <task_id>

# 查看谱系
python -m aios --config config.json evolution lineage-list
python -m aios --config config.json evolution lineage-show lin_bf4054b853224d09

# 让当前谱系再做一次自主决策
python -m aios --config config.json evolution lineage-run lin_bf4054b853224d09

# Capsule 与 Experiment
python -m aios --config config.json capsule list
python -m aios --config config.json experiment show <experiment_id>

# Skill / Candidate
python -m aios --config config.json skill list
python -m aios --config config.json skill candidates
python -m aios --config config.json evolution list

# 完整测试
$env:PYTHONPATH='src'
python -m unittest discover -s tests -p 'test_*.py'
```

## 14. 当前阶段结论

当前最准确的状态是：

```text
Task Runtime                     已具备实际执行能力
Tool / Resource / Sandbox        基本成熟
State / Trace / Capsule          基本成熟
Component / Capability Model     已建立统一描述
Free Runtime                     已运行
Autonomous Lineage               已能产生和继承变化
Counterfactual Measurement       已真实跑通
Skill Autonomous Authoring       尚未稳定成功
Cross-task Self-Optimization     尚未证明
Autonomous Runtime Repair        尚未证明
```

AIOS 现在已经能“产生不同版本的自己并测量后果”，但还不能可靠地“选出真正更好的自己”。最新 read-ledger 实验非常有价值，因为它证明了谱系系统不仅能记录正面结果，也能揭示负迁移。下一阶段最重要的不是继续增加 Reasoner 合同，而是让测量事实完整、无效实验不会污染结论，并为昂贵实验设置独立边界。
